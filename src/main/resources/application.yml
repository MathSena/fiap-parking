spring:
  datasource:
    url: jdbc:mysql://localhost:3307/userms-mysql
    username: dev
    password: fiap
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
